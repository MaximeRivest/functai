
```{python}
from functai import ai, _ai
from typing import List, Literal

@ai(lm="gpt-4.1")
def analyze(text: str) -> Literal['good', 'bad']:
    "A function that does sentiment analysis"
    think: str = _ai["think!"]
    return _ai
```



```{python}
analyze("this is great", _prediction = True)
```


```{python}
analyze.__dspy__.program._sig
```