
```{python}
import functai
from functai import ai, _ai

functai.settings.lm = "gpt-4.1"

@ai
def good_or_bad(text_for_analysis):
    ...

good_or_bad("that is a great library!")
```

```{python}
@ai
def good_or_bad(text_for_analysis):
    return _ai

good_or_bad("Use AI to fulfill the function! Cool :)")
```


```{python}
@ai
def good_or_bad(text_for_analysis):
    reasoning = _ai
    return _ai

good_or_bad("Wow it can think too!")
```


```{python}
good_or_bad("Wow it can think too!", _prediction = True)
```

```{python}
from typing import Literal

def positivity_meter(text):
    "A tool that does sentiment analysis"
    return "good!"

@ai(tools = [positivity_meter])
def good_or_bad(text_for_analysis) -> Literal['good', 'bad']:
    list_of_neg_words = _ai
    return _ai

good_or_bad("That's is horrible", _prediction = True)
```

```{python}
import functai
from functai import ai, _ai

functai.settings.lm = "gpt-4.1"

@ai
def sentiment_analyzer(text_for_analysis):
    ...

sentiment_analyzer("that is a great library!")
```


```{python}
import functai
from functai import ai, _ai

functai.settings.lm = "gpt-4.1"

@ai
def sentiment_analyzer(text_for_analysis):
    _ai

sentiment_analyzer("that is a great library!")
```


```{python}
import functai
from functai import ai, _ai
from typing import List, Literal

functai.settings.lm = "gpt-4.1"

@ai
def sentiment_analyzer(text_for_analysis) -> Literal['good', 'bad']:
    "A function that does sentiment analysis"
    think: str = _ai
    sentiment = _ai
    return sentiment

```



```{python}
sentiment_analyzer("this is great!!")
```


```{python}
import functai
print(functai.inspect_history_text())
```